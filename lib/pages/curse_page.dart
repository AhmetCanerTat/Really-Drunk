import 'package:flutter/material.dart';

class CursePage extends StatelessWidget {
  const CursePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("CURSED!"),
      ),
    );
  }
}
